name: CI Pipeline for Selenium Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  call-tests:
    uses: ./.github/workflows/reusable-test.yml
